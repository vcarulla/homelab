# Speedtest Tracker Secrets
# Copy to: speedtest-tracker/secrets.env
# Generate APP_KEY with: php artisan key:generate --show
# Or use: echo "base64:$(openssl rand -base64 32)"

APP_KEY=base64:REPLACE_WITH_YOUR_GENERATED_KEY
